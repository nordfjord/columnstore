version: "2"

linters:
  default: standard
  enable:
    - errcheck
    - modernize
    - staticcheck
    - unused

  exclusions:
    presets:
      - std-error-handling
      - common-false-positives
